# Yarn

**Yarn** is a fast, reliable, and secure dependency management application.

## Installation

Once you have **[npm](npm.md)** installed you can run the following both to install and upgrade Yarn:

```shell
npm install --global yarn
```

Check that Yarn is installed by running:

```shell
yarn --version
```
